# Python packages for the RITM label scanner.
#
# You ALSO need the Tesseract OCR *engine* installed separately — it is not a
# pip package. See README.md ("Install") for the Windows/Linux steps.
pytesseract>=0.3.10
opencv-python-headless>=4.8
numpy>=1.24
Pillow>=10.0
